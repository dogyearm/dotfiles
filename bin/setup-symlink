#!/bin/bash

# Symlink
dotfiles=(.zshrc .zshenv .vimrc .tmux.conf .gitconfig .gitignore .bin)

for file in "${dotfiles[@]}"; do
  sourceFile="$(pwd)/$file"
  targetFile="$HOME/$file"
  ln -snfv $sourceFile $targetFile
done
